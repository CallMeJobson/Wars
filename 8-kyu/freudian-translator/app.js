function toFreud(string) {
    if(string === ""){
      return"";
    }
   let result = ""
   let count = string.split(" ").length
   
   for(let x = 0; x < count; x++){
      result += " sex"
    }
  return result.trim();
  }